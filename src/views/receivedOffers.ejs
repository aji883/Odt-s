<%- include('partials/header', { title: title, bodyClass: bodyClass }) %>

<div class="profile-container">

    <aside class="profile-sidebar">
        <% if (user) { %> 
            <div class="user-info">
                <img src="/<%= user.profilePicture || 'uploads/default-avatar.png' %>" alt="<%= user.username %>" class="user-avatar">
                <h2 class="user-name"><%= user.username %></h2>
                <p class="user-email"><%= user.email %></p> 
                <p class="user-phone"><%= user.phoneNumber || 'No. Telepon belum diatur' %></p>
            </div>
            <nav class="profile-nav">
                <a href="/items/new" class="nav-button"><span>➕</span> Tambah Item Baru</a>
                <a href="/user/profile" class="nav-button"><span>👕</span> Koleksi Saya</a>
                <a href="/user/favorites" class="nav-button"><span>⭐</span> Item Favorit</a>
                <a href="/transactions/received" class="nav-button active"><span>🔔</span> Notifikasi</a>
                <a href="/user/profile/edit" class="nav-button"><span>✏️</span> Edit Profil</a>
                <a href="/auth/logout" class="nav-button"><span>🚪</span> Logout</a>
            </nav>
        <% } %>
    </aside>

    <div class="profile-main">
        <div class="main-header-block">
            <h1>Notifikasi</h1>
            <p>Pilih jenis notifikasi yang ingin Anda lihat.</p>
        </div>

        <div class="notification-nav-grid">
            
            <a href="/transactions/received/incoming" class="notification-card">
                <div class="card-icon">📥</div>
                <h3>Pesanan Terima</h3>
                <p>Pesanan dari pengguna lain untuk item Anda.</p>
                </a>

            <a href="/transactions/received/outgoing" class="notification-card">
                <div class="card-icon">📤</div>
                <h3>Pesanan Proses</h3>
                <p>Status pesanan yang Anda kirim ke pengguna lain.</p>
            </a>

        </div>
    </div>
</div>
<%- include('partials/footer') %>

<style>
    .notification-nav-grid {
        display: grid;
        grid-template-columns: 1fr 1fr; /* Dua kolom sama besar */
        gap: 2rem;
    }

    .notification-card {
        display: block;
        padding: 2.5rem;
        text-decoration: none;
        color: #fff;
        background: var(--glass-bg, rgba(255,255,255,0.1));
        border: 1px solid var(--glass-border, rgba(255,255,255,0.2));
        border-radius: 15px;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .notification-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 30px rgba(0,0,0,0.3);
    }

    .notification-card .card-icon {
        font-size: 2.5rem;
        margin-bottom: 1rem;
    }

    .notification-card h3 {
        font-size: 1.5rem;
        margin: 0 0 0.5rem 0;
        color: #fff;
    }

    .notification-card p {
        font-size: 1rem;
        color: #ccc;
        margin: 0;
    }

    /* Responsif untuk layar kecil */
    @media (max-width: 992px) {
        .notification-nav-grid {
            grid-template-columns: 1fr; /* Satu kolom di layar kecil */
        }
    }
</style>