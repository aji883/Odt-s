<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Admin</title>

    <style>
        /* --- Global & Theme --- */
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            margin: 0;
            background-color: #1e1e2f; /* Background dashboard gelap */
            color: #f0f0f0;
            line-height: 1.6;
        }

        .admin-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 2rem;
        }

        /* --- Admin Header & Navigasi --- */
        .admin-header {
            background-color: #2a2a40; /* Warna panel */
            padding: 1.5rem 2rem;
            border-radius: 10px;
            margin-bottom: 2rem;
            border: 1px solid #3a3a5a;
        }

        .admin-header h1 {
            margin: 0 0 1rem 0;
            color: #fff;
            font-size: 1.8rem;
        }

        .admin-nav {
            display: flex;
            flex-wrap: wrap;
            align-items: center; /* Sejajarkan tombol */
            gap: 0.5rem;
        }

        .admin-nav a {
            color: #ccc;
            text-decoration: none;
            margin-right: 15px;
            padding: 8px 12px;
            border-radius: 5px;
            font-size: 0.9em;
            font-weight: 500;
            transition: background-color 0.2s ease, color 0.2s ease;
        }

        .admin-nav a:hover {
            background-color: #3a3a5a;
            color: #fff;
        }
        .admin-nav a.active {
            background-color: #7b40f5; /* Warna aksen (ungu) */
            color: #fff;
        }
        
        /* --- Tombol Logout --- */
        .admin-nav a.logout {
            margin-left: auto; /* Dorong ke kanan */
            color: #f39c12;
        }
        .admin-nav a.logout:hover {
            background-color: #f39c12;
            color: #1e1e2f;
        }

        /* --- Dashboard Grid (Halaman Utama Admin) --- */
        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 1.5rem;
        }

        .dashboard-card {
            background-color: #2a2a40;
            border-radius: 10px;
            padding: 1.5rem;
            text-decoration: none;
            color: #f0f0f0;
            border: 1px solid #3a3a5a;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        .dashboard-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.3);
        }

        .dashboard-card h3 {
            margin-top: 0;
            color: #fff;
            font-size: 1.4rem;
            border-bottom: 1px solid #3a3a5a;
            padding-bottom: 0.5rem;
        }

        .dashboard-card p {
            color: #aaa;
            font-size: 0.9em;
            margin-bottom: 1.5rem;
        }

        .dashboard-card .card-count {
            font-size: 2rem;
            font-weight: 700;
            color: #fff;
        }
    </style>
</head>
<body>
    <div class="admin-container">
        <header class="admin-header">
            <h1>Dashboard Admin</h1>
            <nav class="admin-nav">
                <a href="/admin/dashboard" class="active">Dashboard</a>
                <a href="/admin/users">Manajemen Pengguna</a>
                <a href="/admin/items">Moderasi Item</a>
                <a href="/auth/logout" class="logout">Logout</a>
            </nav>
        </header>
        
        <main class="dashboard-grid">
            <a href="/admin/users" class="dashboard-card">
                <h3>Manajemen Pengguna</h3>
                <p>Lihat, blokir, atau hapus pengguna yang melanggar.</p>
                <span class="card-count"><%= typeof userCount != 'undefined' ? userCount : '0' %> Users</span> 
            </a>
            <a href="/admin/items" class="dashboard-card">
                <h3>Moderasi Item</h3>
                <p>Lihat dan hapus postingan item yang tidak pantas.</p>
                <span class="card-count"><%= typeof itemCount != 'undefined' ? itemCount : '0' %> Items</span>
            </a>
        </main>
    </div>
</body>
</html>